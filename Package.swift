// swift-tools-version:5.0

import PackageDescription

let package = Package(
    name: "DictionaryCoding",
    products: [
        .library(
            name: "DictionaryCoding",
            targets: ["DictionaryCoding"]),
    ],
    targets: [
        .target(
            name: "DictionaryCoding",
            dependencies: []),
    ]
)
